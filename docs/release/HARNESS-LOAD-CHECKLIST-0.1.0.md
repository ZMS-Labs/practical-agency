# Per-harness load checklist — 0.1.0

Climb tip of record (hardened kernel + 1.0 definition docs). Fill only from live observation at tip
`a2258539e74e7d3eb59dfc1d9c24b8082994ab8f` (or record a new tip SHA).

## Harness: ________

```text
source revision:
installed path:
loaded skill count:
loaded skill name:
loaded description present and exact: yes/no
"manifest this" invocation accepted: yes/no
"helix it" compatibility intent accepted: yes/no
cache/reload behavior:
verification tier: live-harness | structural-archive-only
observer:
date:
```

Closing **P2-HARNESS-LOAD** requires at least one live-harness tier record for
each harness named in installation docs (currently: Cursor, generic Agent
Skills layout; Claude plugin metadata present).
